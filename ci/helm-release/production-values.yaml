replicaCount: 2
ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: "nginx"
    kubernetes.io/tls-acme: "true"
  hosts:
    - production.swarm.pw
  tls:
    - secretName: production.swarm.pw-tls
      hosts:
        - production.swarm.pw
